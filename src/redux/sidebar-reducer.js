let initialState = {
    friends: [
        {id: 1, name: 'Anna'},
        {id: 2, name: 'Pavel'},
        {id: 3, name: 'Nikita'}
    ]
};

const sidebarReducer = (state=initialState, action) => {

    return state
}

export default sidebarReducer;
